# Hoist all packages so React Native / Expo resolvers can find them
# at the root node_modules without needing symlink traversal.
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
# Allow workspaces to declare engines that don't exactly match the host Node version.
engine-strict=false
